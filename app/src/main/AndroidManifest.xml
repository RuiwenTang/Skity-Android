<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SkityAndroid">
        <activity
            android:name=".BackendCompareActivity"
            android:exported="false"
            android:label="@string/title_backend_compare" />

        <activity
            android:name=".CapabilityInfoActivity"
            android:exported="false"
            android:label="@string/title_capability_info" />

        <activity
            android:name=".SceneGalleryActivity"
            android:exported="false"
            android:label="@string/title_scene_gallery" />

        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
